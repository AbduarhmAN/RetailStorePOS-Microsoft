<Window x:Class="RetailStorePOS.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:RetailStorePOS.App"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Nexill POS"
        Height="768"
        Width="1024"
        MinHeight="600"
        MinWidth="480"
        Icon="Resources/Images/logo.png"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        SourceInitialized="Window_SourceInitialized"
        StateChanged="Window_StateChanged"
        PreviewKeyDown="Window_PreviewKeyDown">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome x:Name="windowChrome"
                            CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="WindowChromeButtonBase" TargetType="Button">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="34" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ChromeBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ChromeBorder" Property="Background" Value="#EDF3FD" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ChromeBorder" Property="Background" Value="#DDE8FF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MinimizeWindowButton" TargetType="Button" BasedOn="{StaticResource WindowChromeButtonBase}">
            <Setter Property="ToolTip" Value="Minimize" />
            <Setter Property="Content">
                <Setter.Value>
                    <iconPacks:PackIconMaterial Kind="WindowMinimize" Width="14" Height="14" Foreground="{StaticResource AppMuted}" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MaximizeWindowButton" TargetType="Button" BasedOn="{StaticResource WindowChromeButtonBase}">
            <Setter Property="ToolTip" Value="Maximize" />
            <Setter Property="Content">
                <Setter.Value>
                    <iconPacks:PackIconMaterial Kind="WindowMaximize" Width="13" Height="13" Foreground="{StaticResource AppMuted}" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RestoreWindowButton" TargetType="Button" BasedOn="{StaticResource WindowChromeButtonBase}">
            <Setter Property="ToolTip" Value="Restore" />
            <Setter Property="Content">
                <Setter.Value>
                    <iconPacks:PackIconMaterial Kind="WindowRestore" Width="13" Height="13" Foreground="{StaticResource AppMuted}" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseWindowButton" TargetType="Button" BasedOn="{StaticResource WindowChromeButtonBase}">
            <Setter Property="ToolTip" Value="Close" />
            <Setter Property="Content">
                <Setter.Value>
                    <iconPacks:PackIconMaterial Kind="Close" Width="13" Height="13" Foreground="{StaticResource AppMuted}" />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ChromeBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ChromeBorder" Property="Background" Value="#E81123" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ChromeBorder" Property="Background" Value="#C50F1F" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ShellIconButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Width" Value="38" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="MinWidth" Value="38" />
            <Setter Property="MinHeight" Value="38" />
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource AppSurface}"
            BorderBrush="{StaticResource AppBorder}"
            BorderThickness="1">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="CornerRadius" Value="18" />
                <Setter Property="Margin" Value="8" />
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="#0D1A2F" BlurRadius="28" ShadowDepth="0" Opacity="0.12" />
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType=Window}}" Value="Maximized">
                        <Setter Property="CornerRadius" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Effect" Value="{x:Null}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid x:Name="MainGrid" Background="#FBFCFE">
            <Grid.RowDefinitions>
                <RowDefinition Height="8" />
                <RowDefinition Height="72" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown" />

            <views:HeroUiShellView x:Name="ShellHost" Grid.Row="1" />

            <Border x:Name="ContentShell"
                    Grid.Row="2"
                    Margin="10"
                    Background="#F7F9FC"
                    BorderBrush="#E5EBF4"
                    BorderThickness="1"
                    CornerRadius="16">
                <Grid x:Name="MainContentArea" Background="#F7F9FC">
                    <views:CheckoutView DataContext="{Binding Checkout}">
                        <views:CheckoutView.Style>
                            <Style TargetType="views:CheckoutView">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsChecked, ElementName=TabCheckout}" Value="True" />
                                            <Condition Binding="{Binding DataContext.CanCheckout, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:CheckoutView.Style>
                    </views:CheckoutView>

                    <views:ProductsView DataContext="{Binding Products}">
                        <views:ProductsView.Style>
                            <Style TargetType="views:ProductsView">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsChecked, ElementName=TabProducts}" Value="True" />
                                            <Condition Binding="{Binding DataContext.CanManageProducts, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:ProductsView.Style>
                    </views:ProductsView>

                    <views:SettingsView DataContext="{Binding Settings}">
                        <views:SettingsView.Style>
                            <Style TargetType="views:SettingsView">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsChecked, ElementName=TabSettings}" Value="True" />
                                            <Condition Binding="{Binding DataContext.CanManageSettings, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:SettingsView.Style>
                    </views:SettingsView>

                    <views:UserManagementView DataContext="{Binding UserManagement}">
                        <views:UserManagementView.Style>
                            <Style TargetType="views:UserManagementView">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsChecked, ElementName=TabUsers}" Value="True" />
                                            <Condition Binding="{Binding DataContext.CanManageUsers, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:UserManagementView.Style>
                    </views:UserManagementView>

                    <views:ReportsView DataContext="{Binding Reports}">
                        <views:ReportsView.Style>
                            <Style TargetType="views:ReportsView">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsChecked, ElementName=TabReports}" Value="True" />
                                            <Condition Binding="{Binding DataContext.CanViewReports, RelativeSource={RelativeSource AncestorType=Window}}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:ReportsView.Style>
                    </views:ReportsView>

                    <views:HelpView DataContext="{Binding Help}">
                        <views:HelpView.Style>
                            <Style TargetType="views:HelpView">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=TabHelp}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:HelpView.Style>
                    </views:HelpView>
                </Grid>
            </Border>

            <StackPanel x:Name="HiddenTabState" Visibility="Collapsed">
                <RadioButton x:Name="TabCheckout" GroupName="MainTabs" IsChecked="True" Checked="MainTab_Checked" />
                <RadioButton x:Name="TabProducts" GroupName="MainTabs" Checked="MainTab_Checked" />
                <RadioButton x:Name="TabReports" GroupName="MainTabs" Checked="MainTab_Checked" />
                <RadioButton x:Name="TabUsers" GroupName="MainTabs" Checked="MainTab_Checked" />
                <RadioButton x:Name="TabSettings" GroupName="MainTabs" Checked="MainTab_Checked" />
                <RadioButton x:Name="TabHelp" GroupName="MainTabs" Checked="MainTab_Checked" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
