<UserControl x:Class="RetailStorePOS.App.CloseDayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800"
              Background="Transparent"
              FontFamily="Inter, Segoe UI Variable Text, Segoe UI">

    <UserControl.Resources>
        <Color x:Key="MacCloseDayPageTop">#F8F9FB</Color>
        <Color x:Key="MacCloseDayPageBottom">#EEF2F6</Color>
        <Color x:Key="MacCloseDaySurface">#F6F8FC</Color>
        <Color x:Key="MacCloseDayCard">#FFFFFF</Color>
        <Color x:Key="MacCloseDayBorder">#DCE4EF</Color>
        <Color x:Key="MacCloseDayTextPrimary">#1C2431</Color>
        <Color x:Key="MacCloseDayTextSecondary">#6A7486</Color>
        <Color x:Key="MacCloseDayAccent">#3B82F6</Color>
        <Color x:Key="MacCloseDayAccentPressed">#2563EB</Color>
        <Color x:Key="MacCloseDaySuccess">#16A34A</Color>
        <Color x:Key="MacCloseDayDanger">#EF4444</Color>

        <LinearGradientBrush x:Key="MacCloseDayPageBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="{StaticResource MacCloseDayPageTop}" Offset="0" />
            <GradientStop Color="{StaticResource MacCloseDayPageBottom}" Offset="1" />
        </LinearGradientBrush>

        <SolidColorBrush x:Key="MacCloseDaySurfaceBrush" Color="{StaticResource MacCloseDaySurface}" />
        <SolidColorBrush x:Key="MacCloseDayCardBrush" Color="{StaticResource MacCloseDayCard}" />
        <SolidColorBrush x:Key="MacCloseDayBorderBrush" Color="{StaticResource MacCloseDayBorder}" />
        <SolidColorBrush x:Key="MacCloseDayTextPrimaryBrush" Color="{StaticResource MacCloseDayTextPrimary}" />
        <SolidColorBrush x:Key="MacCloseDayTextSecondaryBrush" Color="{StaticResource MacCloseDayTextSecondary}" />
        <SolidColorBrush x:Key="MacCloseDayAccentBrush" Color="{StaticResource MacCloseDayAccent}" />
        <SolidColorBrush x:Key="MacCloseDayAccentPressedBrush" Color="{StaticResource MacCloseDayAccentPressed}" />
        <SolidColorBrush x:Key="MacCloseDaySuccessBrush" Color="{StaticResource MacCloseDaySuccess}" />
        <SolidColorBrush x:Key="MacCloseDayDangerBrush" Color="{StaticResource MacCloseDayDanger}" />

        <Style x:Key="CloseDayPanelStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource MacCloseDayCardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource MacCloseDayBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="16" />
        </Style>

        <Style x:Key="CloseDayCardStyle" TargetType="Border" BasedOn="{StaticResource CloseDayPanelStyle}">
            <Setter Property="Padding" Value="18" />
        </Style>

        <Style x:Key="CloseDayTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="31" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource MacCloseDayTextPrimaryBrush}" />
        </Style>

        <Style x:Key="CloseDaySubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource MacCloseDayTextSecondaryBrush}" />
        </Style>

        <Style x:Key="CloseDayMetricLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource MacCloseDayTextSecondaryBrush}" />
        </Style>

        <Style x:Key="CloseDayMetricValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="30" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource MacCloseDayTextPrimaryBrush}" />
        </Style>

        <Style x:Key="CloseDayBaseButtonStyle" TargetType="Button">
            <Style.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" />
                </Style>
            </Style.Resources>
            <Setter Property="MinHeight" Value="40" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.84" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseDayPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource CloseDayBaseButtonStyle}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource MacCloseDayAccentBrush}" />
            <Setter Property="BorderBrush" Value="#2563EB" />
        </Style>

        <Style x:Key="CloseDaySecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource CloseDayBaseButtonStyle}">
            <Setter Property="Foreground" Value="{StaticResource MacCloseDayTextPrimaryBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#DCE4EF" />
        </Style>

        <Style x:Key="CloseDayDangerButtonStyle" TargetType="Button" BasedOn="{StaticResource CloseDayBaseButtonStyle}">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource MacCloseDayDangerBrush}" />
            <Setter Property="BorderBrush" Value="#D52E24" />
            <Setter Property="MinHeight" Value="48" />
            <Setter Property="Padding" Value="20,10" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource MacCloseDayPageBrush}">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CloseDayPanelStyle}" Padding="18,14" Margin="0,0,0,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <iconPacks:PackIconMaterial Kind="CashRegister" Width="26" Height="26" Foreground="{StaticResource MacCloseDayAccentBrush}" Margin="0,0,10,0" VerticalAlignment="Center" />
                        <StackPanel>
                            <TextBlock Text="Close Day" Style="{StaticResource CloseDayTitleStyle}" />
                            <TextBlock Text="Finalize daily totals and print the Z-Report" Style="{StaticResource CloseDaySubtitleStyle}" />
                        </StackPanel>
                    </StackPanel>

                    <Button Grid.Column="1" Style="{StaticResource CloseDaySecondaryButtonStyle}" Command="{Binding LoadSummaryCommand}" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconMaterial Kind="Refresh" Width="14" Height="14" Margin="0,0,6,0" />
                            <TextBlock Text="Refresh Totals" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CloseDayCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Total Revenue" Style="{StaticResource CloseDayMetricLabelStyle}" />
                        <TextBlock Text="{Binding TotalRevenue, StringFormat={}{0:C}}" Style="{StaticResource CloseDayMetricValueStyle}" Foreground="{StaticResource MacCloseDaySuccessBrush}" Margin="0,4,0,0" />
                        <TextBlock Text="{Binding TransactionCount, StringFormat={}{0} transactions}" Style="{StaticResource CloseDaySubtitleStyle}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CloseDayCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Cash in Drawer" Style="{StaticResource CloseDayMetricLabelStyle}" />
                        <TextBlock Text="{Binding CashTotal, StringFormat={}{0:C}}" Style="{StaticResource CloseDayMetricValueStyle}" Foreground="{StaticResource MacCloseDayAccentBrush}" Margin="0,4,0,0" />
                        <TextBlock Text="Amount to deposit" Style="{StaticResource CloseDaySubtitleStyle}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="4" Style="{StaticResource CloseDayCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Card Sales" Style="{StaticResource CloseDayMetricLabelStyle}" />
                        <TextBlock Text="{Binding CardTotal, StringFormat={}{0:C}}" Style="{StaticResource CloseDayMetricValueStyle}" Margin="0,4,0,0" />
                        <TextBlock Text="Captured by terminal" Style="{StaticResource CloseDaySubtitleStyle}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="6" Style="{StaticResource CloseDayCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Tax Collected" Style="{StaticResource CloseDayMetricLabelStyle}" />
                        <TextBlock Text="{Binding TotalTax, StringFormat={}{0:C}}" Style="{StaticResource CloseDayMetricValueStyle}" Margin="0,4,0,0" />
                        <TextBlock Text="Remit to tax authority" Style="{StaticResource CloseDaySubtitleStyle}" Margin="0,4,0,0" />
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2"
                    Background="#F4F8FE"
                    BorderBrush="#DFE6F2"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="12,9"
                    Margin="0,0,0,14">
                <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MacCloseDayTextSecondaryBrush}" FontWeight="SemiBold" TextWrapping="Wrap" />
            </Border>

            <Border Grid.Row="3" Style="{StaticResource CloseDayPanelStyle}" Padding="20" VerticalAlignment="Top">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <iconPacks:PackIconMaterial Kind="AlertCircleOutline" Width="18" Height="18" Foreground="#D97706" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="End of Day Procedure" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource MacCloseDayTextPrimaryBrush}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="Close Shift finalizes the day, prints the Z-Report, and resets daily counters for the next session." Style="{StaticResource CloseDaySubtitleStyle}" TextWrapping="Wrap" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource CloseDaySecondaryButtonStyle}" Command="{Binding PrintZReportCommand}" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="PrinterOutline" Width="14" Height="14" Margin="0,0,6,0" />
                                <TextBlock Text="Print Z-Report" />
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource CloseDayDangerButtonStyle}" Command="{Binding CloseShiftCommand}">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="CashRegister" Width="16" Height="16" Margin="0,0,7,0" />
                                <TextBlock Text="Close Shift" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
