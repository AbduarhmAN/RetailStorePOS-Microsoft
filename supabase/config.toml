# Minimal function configuration recovered with the deployed licensing source.
# This file does not mirror the remote database or authentication settings.
project_id = "2.0.0"

[functions.license-api]
verify_jwt = false

[functions.activate-install]
verify_jwt = false
