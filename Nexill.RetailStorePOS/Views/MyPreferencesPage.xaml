<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RetailStorePOS.WinUiLogin.Views.MyPreferencesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RetailStorePOS.WinUiLogin.Views"
    xmlns:common="using:RetailStorePOS.WinUiLogin.Common"
    Background="Transparent">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Section Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock x:Uid="MyPreferences_Header_Title" Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiBold"/>
                <TextBlock x:Uid="MyPreferences_Header_Subtitle"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="24">
                
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6" Padding="20">
                    <StackPanel Spacing="16">
                        <TextBlock x:Uid="MyPreferences_QuickCash_Title" FontWeight="SemiBold" FontSize="16"/>
                        <TextBlock x:Uid="MyPreferences_QuickCash_Subtitle" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,-12,0,0"/>
                        
                        <Grid ColumnSpacing="24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <NumberBox
                                       Grid.Column="0"
                                       x:Uid="MyPreferences_QuickCash_Value1"
                                       Value="{x:Bind ViewModel.QuickCash1Value, Mode=TwoWay}"
                                       Minimum="0"
                                       SmallChange="1"
                                       AcceptsExpression="False"
                                       SpinButtonPlacementMode="Hidden"
                                       CornerRadius="4"
                                       common:NumericInputNormalization.NormalizeOnLostFocus="True"/>
                            <NumberBox Grid.Column="1" x:Uid="MyPreferences_QuickCash_Value2" Value="{x:Bind ViewModel.QuickCash2Value, Mode=TwoWay}" Minimum="0" SmallChange="1" AcceptsExpression="False" SpinButtonPlacementMode="Hidden" CornerRadius="4" common:NumericInputNormalization.NormalizeOnLostFocus="True" />
                            <NumberBox Grid.Column="2" x:Uid="MyPreferences_QuickCash_Value3" Value="{x:Bind ViewModel.QuickCash3Value, Mode=TwoWay}" Minimum="0" SmallChange="1" AcceptsExpression="False" SpinButtonPlacementMode="Hidden" CornerRadius="4" common:NumericInputNormalization.NormalizeOnLostFocus="True" />
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Status readout -->
                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" 
                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" 
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
