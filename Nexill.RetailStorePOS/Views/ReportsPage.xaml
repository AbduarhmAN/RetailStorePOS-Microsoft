<Page
    x:Class="RetailStorePOS.WinUiLogin.Views.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <NavigationView x:Name="ReportsNavView" 
                        PaneDisplayMode="Top" 
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        SelectionChanged="NavView_SelectionChanged"
                        Margin="0,0,0,1">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="Reports_Nav_Dashboard" Tag="Dashboard" IsSelected="True">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Library"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="Reports_Nav_Receipts" Tag="Receipts">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Folder"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <NavigationView.PaneFooter>
                <StackPanel Orientation="Horizontal" Padding="12,0" Spacing="8">
                    <ProgressRing x:Name="ReadinessProgressRing" IsActive="False" Visibility="Collapsed" Margin="12,0,0,0" Width="20" Height="20" VerticalAlignment="Center"/>
                    <TextBlock x:Name="ReadinessStatusText" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="Gray"/>
                </StackPanel>
            </NavigationView.PaneFooter>
        </NavigationView>

        <Frame x:Name="ReportsContentFrame" Grid.Row="1" IsNavigationStackEnabled="False" />
    </Grid>
</Page>
