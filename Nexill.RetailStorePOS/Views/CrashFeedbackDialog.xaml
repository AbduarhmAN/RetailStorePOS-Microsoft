<ContentDialog
    x:Class="RetailStorePOS.WinUiLogin.Views.CrashFeedbackDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DefaultButton="Primary"
    RequestedTheme="Default"
    CornerRadius="8"
    FlowDirection="{x:Bind Loc.FlowDirection, Mode=OneWay}"
    PrimaryButtonText="{x:Bind CrashFeedbackDialog_PrimaryButtonText, Mode=OneWay}"
    SecondaryButtonText="{x:Bind CrashFeedbackDialog_SecondaryButtonText, Mode=OneWay}">

    <ContentDialog.Title>
        <TextBlock Text="{x:Bind CrashFeedbackDialog_Title, Mode=OneWay}" />
    </ContentDialog.Title>

    <StackPanel Spacing="16" Width="400">
        <TextBlock Text="{x:Bind CrashFeedbackDialog_IntroText, Mode=OneWay}" 
                   TextWrapping="Wrap" />

        <RadioButtons x:Name="IssueCategoryRadioGroup" SelectedIndex="0">
            <RadioButton Content="{x:Bind CrashFeedbackDialog_Checkout, Mode=OneWay}" Tag="checkout" />
            <RadioButton Content="{x:Bind CrashFeedbackDialog_Products, Mode=OneWay}" Tag="products" />
            <RadioButton Content="{x:Bind CrashFeedbackDialog_Reports, Mode=OneWay}" Tag="reports" />
            <RadioButton Content="{x:Bind CrashFeedbackDialog_Settings, Mode=OneWay}" Tag="settings" />
            <RadioButton Content="{x:Bind CrashFeedbackDialog_Startup, Mode=OneWay}" Tag="startup" />
            <RadioButton Content="{x:Bind CrashFeedbackDialog_Other, Mode=OneWay}" Tag="other" />
        </RadioButtons>

        <TextBox x:Name="DetailsTextBox" 
                 Header="{x:Bind CrashFeedbackDialog_DetailsTextBoxHeader, Mode=OneWay}" 
                 PlaceholderText="{x:Bind CrashFeedbackDialog_DetailsTextBoxPlaceholder, Mode=OneWay}" 
                 AcceptsReturn="True" 
                 TextWrapping="Wrap"
                 Height="80" />

        <CheckBox x:Name="SuppressPromptCheckBox" 
                  Content="{x:Bind CrashFeedbackDialog_SuppressPromptCheckBox, Mode=OneWay}" 
                  Margin="0,8,0,0"/>
    </StackPanel>
</ContentDialog>
