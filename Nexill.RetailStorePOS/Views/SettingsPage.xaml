<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="RetailStorePOS.WinUiLogin.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RetailStorePOS.WinUiLogin.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid x:Name="RootLayout">
        <NavigationView x:Name="SettingsNavView"
                        PaneDisplayMode="LeftCompact"
                        IsPaneToggleButtonVisible="False"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        SelectionChanged="SettingsNavView_SelectionChanged"
                        Background="Transparent"
                        ItemInvoked="SettingsNavView_ItemInvoked">

            <NavigationView.PaneFooter>
                <Grid Padding="8,8,8,12">
                    <Button x:Name="SettingsPaneToggleButton"
                            Width="36"
                            Height="36"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Background="#FFFFFF"
                            BorderBrush="#D8E1EF"
                            BorderThickness="1"
                            CornerRadius="6"
                            Click="SettingsPaneToggleButton_Click">
                        <FontIcon Glyph="&#xE700;"
                                  FontSize="14"
                                  Foreground="#4B5563" />
                    </Button>
                </Grid>
            </NavigationView.PaneFooter>
            
            <NavigationView.MenuItems>
                <!-- Main Admin Settings -->
                <NavigationViewItem x:Name="StoreNav" Content="Store Management" Tag="store">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Shop"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem x:Name="TaxNav" Content="Tax Configuration" Tag="tax">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Calculator"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItem x:Name="UsersNav" Content="Users &amp; Roles" Tag="users">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="People"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                
                <NavigationViewItemSeparator />

                <!-- User Preferences -->
                <NavigationViewItem x:Name="PrefsNav" Content="My Preferences" Tag="prefs">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="ContactInfo"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <!-- Inner Router Frame -->
            <Frame x:Name="SettingsContentFrame" Background="Transparent" />

        </NavigationView>

        <TeachingTip x:Name="SettingsSubNavigationTip"
                     Target="{x:Bind SettingsPaneToggleButton, Mode=OneWay}"
                     Title="Settings navigation"
                     Subtitle="Use this button to show the full settings navigation."
                     PreferredPlacement="Top"
                     IsLightDismissEnabled="False"
                     CloseButtonContent="Skip"
                     CloseButtonClick="SettingsSubNavigationTip_CloseButtonClick" />
    </Grid>
</Page>
